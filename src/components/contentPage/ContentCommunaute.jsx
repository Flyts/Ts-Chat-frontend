import { useContext } from "react"
import { dataContext } from "../../data/context"
import "../../public/css/partials/_contentCommunaute.css"

function ContentCommunaute()
{
    const {
    } = useContext(dataContext)

    const component = 
    <div id="ContentCommunaute">
        
    </div>

    return component
}

export default ContentCommunaute